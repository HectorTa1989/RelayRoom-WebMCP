// Pulled from apps/room/src/styles.css so the film reads as the same product.
export const theme = {
  ink: '#0a0b10',
  inkSoft: '#14161f',
  paper: '#f6f6f8',
  indigo: '#635bff',
  indigoSoft: '#9d98ff',
  amber: '#ff9f0a',
  mint: '#34c759',
  white: '#ffffff',
  muted: 'rgba(255,255,255,0.66)',
  faint: 'rgba(255,255,255,0.4)',
} as const;

export const font =
  '"Segoe UI Variable Display", "Segoe UI", system-ui, -apple-system, "Helvetica Neue", Arial, sans-serif';

export const originColors = ['#635bff', '#c86bfa', '#ff9f0a'] as const;
